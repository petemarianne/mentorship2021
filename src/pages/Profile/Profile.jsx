import React from 'react';
import Layout from '../../components/Layout/Layout';

const Profile = () => {
    return (
        <Layout>
            <div>profile</div>
        </Layout>
    );
};

export default Profile;